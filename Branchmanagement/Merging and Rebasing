# Merge branch
git checkout main
git merge feature-branch

# Rebase branch
git checkout feature-branch
git rebase main

# Resolve conflicts
git mergetool
git add .              # After resolving conflicts
git rebase --continue
